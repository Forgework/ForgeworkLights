led_count = 14
max_current_amps = 2.0
max_brightness = 0.7

# Gamma correction for color space conversion
# 0.45 (default): Degamma sRGB → linear RGB (recommended for most LEDs)
# 1.0: No gamma correction (passthrough)
# 2.2: Gamma encoding (inverse of default, makes colors darker)
gamma_exponent = 0.45

color_order = "RGB"
tool_path = "/usr/bin/framework_tool"
